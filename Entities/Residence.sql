CREATE TABLE Residence (
    Address varchar(255) PRIMARY KEY,
    City varchar(255),
    Type varchar(255),
    Province varchar(20),
    PostalCode varchar(10),
    PhoneNumber varchar(50),
    Bedrooms int
);