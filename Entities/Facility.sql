CREATE TABLE Facility (
    Name varchar(255) PRIMARY KEY,
    Type varchar(255),
    Province varchar(255),
    City varchar(255),
    Address varchar(255),
    Capacity int,
    Website varchar(255),
    Phone varchar(50),
    PostalCode varchar(10)
);